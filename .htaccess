#
# 1. use res/generatepasswd.php
# 1.5 uncomment the following lines
# 2. ???
# 3. HTTP authentication!

#AuthType Basic
#AuthName "Bunzilla"
#AuthUserFile /var/www/bunzilla/res/.htpasswd
#Require valid-user

Options -MultiViews

RewriteEngine On
#
# mod_rewrite is ~fun~
#
# so you probably want to just uncomment this line:
#RewriteBase /bunzilla

# and comment all of these out, unless you're me or something
RewriteCond %{HTTP_HOST} ^meta\.bunzilla\.ga$ [NC]
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.+)$ /index.php?url=$1 [QSA,NC,L]

RewriteCond %{HTTP_HOST} ^var\.abl\.cl$ [NC]
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.+)$ /bunzilla/index.php?url=$1 [QSA,NC,L]
